// const a: readonly [price: 500, itemName: string] = [500, 'T-Shirt']

// b[0] = 400

// b[1].toUpperCase()

let a = ['1', "2", 56]
// const result = a.filter((el): el is number => typeof el === "number");
// const result = a.filter((el) => typeof el === "number");